1. First Install Python (preferably, 2.7) in your computer, in case you don't already have it

2. Then run either in the terminal (Linux/Mac) in this folder the command "python center.py" or using the IDLE 
